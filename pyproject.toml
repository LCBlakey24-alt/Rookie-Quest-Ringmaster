[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rookie-quest-ringmaster"
version = "0.1.0"
description = "Framework prototype for a wrestling manager sim"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]


[project.scripts]
ringmaster = "ringmaster.cli:main"
